// Démarre vide: aucun tool exposé.
// Tu pourras en ajouter plus tard en suivant l'exemple commenté.
export const tools = {
  // hello: async ({ name }) => {
  //   if (!name) throw new Error("Missing 'name'");
  //   return { message: `Salut ${name} 👋` };
  // },
};
